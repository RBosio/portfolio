<nav class="sticky top-0 z-10">
  <ul class="flex justify-center items-center text-white gap-12 text-lg p-2">
    <li>
      <a class="p-2 hover:cursor-pointer hover:underline" id="home" href="#"
        >Inicio</a
      >
    </li>
    <li>
      <a class="p-2 hover:cursor-pointer hover:underline" href="#skills"
        >Skills</a
      >
    </li>
    <li>
      <a class="p-2 hover:cursor-pointer hover:underline" href="#projects"
        >Proyectos</a
      >
    </li>
    <li>
      <a class="p-2 hover:cursor-pointer hover:underline" href="#certifications"
        >Certificaciones</a
      >
    </li>
    <li>
      <a class="p-2 hover:cursor-pointer hover:underline" href="#about"
        >Sobre mi</a
      >
    </li>
  </ul>
  <div class="progress bg-yellow-400 h-2"></div>
</nav>

<style>
  @keyframes progress {
    0% {
      width: 0;
    }
    100% {
      width: 100%;
    }
  }

  @keyframes navbar {
    100% {
      background-color: rgba(255, 255, 0, 0.4);
      color: white;
      backdrop-filter: blur(5px);
      font-size: 20px;
    }
  }

  .progress {
    animation: progress auto linear;
    animation-timeline: scroll(root block);
  }

  ul {
    animation: navbar auto linear both;
    animation-timeline: scroll(root block);
    animation-range: 0 100px;
  }
</style>

<script>
  const home = document.getElementById("home");

  home?.addEventListener("click", () => {
    document.body.scrollTop = 0; // Safari
    document.documentElement.scrollTop = 0; // Chrome, Firefox, IE and Opera
  });
</script>
