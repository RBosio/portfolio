<nav class="sticky top-0 z-10">
  <ul class="flex justify-center items-center text-white gap-12 text-lg">
    <li class="p-2 hover:cursor-pointer hover:underline">
      <a href="#home">Inicio</a>
    </li>
    <li class="p-2 hover:cursor-pointer hover:underline">
      <a href="#about">Sobre mi</a>
    </li>
    <li class="p-2 hover:cursor-pointer hover:underline">
      <a href="#projects">Proyectos</a>
    </li>
    <li class="p-2 hover:cursor-pointer hover:underline" #contact>
      <a href="#contact">Contacto</a>
    </li>
  </ul>
  <div class="progress bg-yellow-400 h-2"></div>
</nav>

<style>
  @keyframes progress {
    0% {
      width: 0;
    }
    100% {
      width: 100%;
    }
  }

  @keyframes navbar {
    100% {
      background-color: rgba(255, 255, 0, 0.4);
      color: white;
      backdrop-filter: blur(5px);
      font-size: 20px;
    }
  }

  .progress {
    animation: progress auto linear;
    animation-timeline: scroll(root block);
  }

  ul {
    animation: navbar auto linear both;
    animation-timeline: scroll(root block);
    animation-range: 0 100px;
  }
</style>
